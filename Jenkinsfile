pipeline {
    agent any

    stages {
        stage('without docker') {
            steps {
                echo 'Hello from without Docker'
                
            }
        }
        
    }
}
